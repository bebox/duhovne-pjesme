verseA = \lyricmode {
  \set stanza = #"1. "
  Init
}

verseB = \lyricmode {
  \set stanza = #"2. "
  Init
}

verseC = \lyricmode {
  \set stanza = #"3. "
  Init
}

verseD = \lyricmode {
  \set stanza = #"4. "
  Init
}
